# Recap
- VCS , Git, Github 
add vs commit  ?
- git clone ? git pull

Branch -> independent line of development
separate workspace created from existing code
pointer to a commit


git branch -> list out all the branches and active branch will have an asterik sign
git branch branchName -> create a new branch
git checkout branchName -> switch to that branch
git checkout -b branchName -> create a new branch and then switch to that branch
git branch -d name -> del the branch

Branching strategy - 

I m testing how the branches work

Clone this project -> https://github.com/prgara/JavaJune26.git
create a feature name from main/master
create a new file and add some content to it
commit those changes 
merge those changes in the main branch

