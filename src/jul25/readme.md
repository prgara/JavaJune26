marks of 5 students in 3 subjects
2D array - it is an arrays inside array. Each element will hold an array inside it which naturally models rows and cols.

Exercise - 1 
create a 2d array with 5 rows and 3 cols and find the sum of each row
total marks of each student


Exercise - 2
Find the empty seat

{0,1,1}, Row -1 
{1,1,0}, Row 2
{0,0,1}
5X3
row1 col1 is empty

To do at home -> Sort the array in ascending and descending order.

static keyword -> it belongs to the class and not to the object and it is a shared entity which will be shared among all the objects of that class
Always access static using class name no need to create the objects.
Static can only be accessed inside a static. 
Static block is always called first
static block then instance block then constructor call.

static variable
static method
static block
static class

Modifiers - access and non-access

Heap - objects are stored here
Stack - 
Method area - static 


String -> it is a character array. it is a class also. non-primitive data type

String literal
using new keyword

Immutability - strings are immutable. 
String constant Pool -> it is inside the heap area. string created with literal will be stored here




 
