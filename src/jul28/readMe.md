static ?
String ?


Using new keyword, it will create one in heap and check in SCP, if exist then ok else will create one in SCP also
Using literal, it will check in SCP, if exists no obj creation else will create in SCP.


 == -> Compare memory addresses
 .equalsTo -> Compare actual content
 
StringBuffer -> mutable, thread safe, slow
StringBuilder -> mutable, not thread safe, fast



 int x = 100;
 
take user input and tell how many vowels are there in the string ... aeiou.... hello -> 2
palindrome or not ..... madam isPalindrome.... 