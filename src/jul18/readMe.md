Class ? Object ?
param vs arg

Instance variable -> belong to the class, have a default value
local var -> they belong to the method, no default value so initialize before using


Create a Product class with name, qty, price
3 methods -> display, discount(num) -> display the discounted price of the product after the said discount, 
bill -> return the total value productPrice*qty
create 2 objects of the Product and call these methods.

Create a calculator class and 4 methods to do sum, sub, multiplication & division.
create and object of calculator class and do all the operations.



Constructor -> it is a special method which is used to initialize the object.
constructor name is always same a class name.
It has no return type not even void.

there are 2 types
- Default constructor
- Param constructor
this keyword -> it refers to the current object.

If there is no constructor in the class then default is always there.
if there is a param constructor then default constructor is not there by default. you have to explicitly define it in order to use it.




Create an Employee class with name, dept, salary. 
create param const to initialize the values and then create 3 objects to print the values.